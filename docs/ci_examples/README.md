# CI Examples Archive

This directory keeps archived CI workflow examples for reference only.

- Files under `docs/ci_examples/workflows/` are **not** the local gate source of truth.
- Local validation source of truth remains:
  - `scripts/fast_check.ps1`
  - `scripts/full_check.ps1`
